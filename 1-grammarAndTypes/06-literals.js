/* 
Literals
*/

let days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];
console.log(days);
console.log(days.toString());

let tired = true;

let soup = {
    a: 'chicken noodle',
    b: 'tomato',
    C: 'beef and barley'
}
console.log(soup.C); // use . to call one of the strings

let car = 'honda';

let dec = 10.2;